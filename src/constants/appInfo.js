export const appInfo = {
    appName: 'projectname',
    dir: '/projectname/',
    apiurl: window.location.origin + '/projectapi',
    defaultUrl: window.location.origin + '/projectname/redirect', //用户登录url，登录后才可获取数据，这里临时用这个
}
